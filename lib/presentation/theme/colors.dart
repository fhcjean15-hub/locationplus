import 'package:flutter/material.dart';

class AppColors {
  // Couleur principale (orange AfricaLocation)
  static const Color primary = Color(0xFFFCA311);

  // Bleu utilisé auparavant
  static const Color secondaryBlue = Color(0xFF14213D);

  // Versions éclaircies du orange (#FCA311)
  static const Color primary50 = Color(0xFFFFE8C2);    // équivalent shade50
  static const Color primary100 = Color(0xFFFFD9A3);
  static const Color primary200 = Color(0xFFFFC985);

  // Noir / gris
  static const Color textDark = Color(0xFF1A1A1A);
  static const Color textLight = Colors.white;
}
